/**
 * Admin - Courses (Programs) Page JavaScript
 *
 * Note: The main functionality has been moved inline to the Blade template
 * for better integration with Laravel routes and CSRF tokens.
 *
 * This file is kept for backwards compatibility only.
 */

/**
 * Initialize the admin courses page
 */
function initAdminCoursesPage() {
  console.log('[courses.js] Page initialization delegated to inline script.');
}

// Export for global access
window.initAdminCoursesPage = initAdminCoursesPage;
